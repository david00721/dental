[project]
name = "template-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Kocsis David" }
]
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.4.4",
    "streamlit>=1.56.0",
]

[project.scripts]
start-dental = "streamlit.web.cli:main"

[build-system]
requires = ["uv_build>=0.11.7,<0.12.0"]
build-backend = "uv_build"
